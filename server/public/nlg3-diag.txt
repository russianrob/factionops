(()=>{let a=[...document.querySelectorAll('button,div[class*=btn],[role=button]')].find(b=>{const t=String(b.textContent||'').trim();return t==='Save Roles'||t==='Save for All Members';});if(!a){console.log('NLG3-DIAG: no Save Roles / Save for All Members button found on page');return;}let form=a;for(let h=0;h<9&&form.parentElement;h++){if(form.querySelectorAll('input[type=checkbox]').length>=2)break;form=form.parentElement;}const cbs=[...form.querySelectorAll('input[type=checkbox]')].slice(0,25).map((cb,i)=>{let c=cb,t='';for(let h=0;h<5&&c;h++){const x=String(c.textContent||'').trim();if(x&&x.length<50){t=x;break;}c=c.parentElement;}return{i,checked:cb.checked,cls:String(cb.className).slice(0,26),label:t.slice(0,40)};});console.log('NLG3-DIAG '+JSON.stringify({formCls:String(form.className||'').slice(0,50),cbCount:cbs.length,checkboxes:cbs,formHtml:String(form.outerHTML||'').replace(/\s+/g,' ').slice(0,500)}));})()
