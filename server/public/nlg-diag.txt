(()=>{const o={checkboxes:[],radios:[],selects:[],buttons:[]};document.querySelectorAll('input[type=checkbox]').forEach((cb,i)=>{if(i>40)return;let lbl='';if(cb.id){const l=document.querySelector('label[for="'+cb.id+'"]');if(l)lbl=l.textContent.trim();}if(!lbl&&cb.closest('label'))lbl=cb.closest('label').textContent.trim();if(!lbl)lbl=String(cb.parentElement&&cb.parentElement.textContent||'').trim();o.checkboxes.push({cls:String(cb.className).slice(0,28),checked:cb.checked,name:cb.name||'',value:String(cb.value||'').slice(0,20),label:lbl.slice(0,40)});});document.querySelectorAll('input[type=radio]').forEach((r,i)=>{if(i>20)return;o.radios.push({checked:r.checked,name:r.name||'',value:String(r.value||'').slice(0,20),label:String((r.closest('label')||r.parentElement||{}).textContent||'').trim().slice(0,40)});});document.querySelectorAll('select').forEach((s,i)=>{if(i>8)return;o.selects.push({cls:String(s.className).slice(0,28),value:String(s.value||'').slice(0,30),opts:[...s.options].slice(0,15).map(x=>x.textContent.trim())});});document.querySelectorAll('button,[role=button],div[class*=btn]').forEach(b=>{const t=(b.textContent||'').trim();if(t&&/send|save|role|member|all|group/i.test(t)&&t.length<30)o.buttons.push(t);});o.buttons=[...new Set(o.buttons)].slice(0,12);console.log('NLG-DIAG '+JSON.stringify(o));})()
